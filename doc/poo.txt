Personas:
características (describen) -> atributos / propiedades -> variables
   peso
   altura
   color de pelo
   color de ojos
acciones (métodos) -> funciones:
   comer()
   dormir()
   reir()
   saltar()
   hablar()

una Persona -> objeto (instancia)
   peso: 69
   altura: 1.67
   color de pelo: moreno
   color de ojos: negros

   persona1 = Persona("nombre",peso, altura)
   persona1.saltar()

La clase es UNA --> pero tenemmos muchos objetos


Constructor: __init__() --> para inicializar
objeto en curso: self
Destructor: __del__() --> para liberar recurso
